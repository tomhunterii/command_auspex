{
  "name": "warhammer-40k",
  "version": "0.1.0",
  "description": "Command Auspex — list-building battle simulator",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "node --test",
    "build:catalogue": "node scripts/build-catalogue.js",
    "tauri:dev": "tauri dev",
    "tauri:build": "npm run build:catalogue && tauri build"
  },
  "type": "module",
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "js-yaml": "^4.1.1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-sql": "^2",
    "bootstrap": "^5.3.8"
  }
}
